model:
  type: "aae"
  hidden_dim: [512]
  latent_dim: 128
  non_linear: true
  learning_rate: 0.0001
  max_learning_rate: 0.005  # For cyclical learning rate
  gamma: 0.98              # Learning rate decay factor

training:
  mode: "direct"
  epochs: 200
  batch_size: 64
  early_stopping_patience: 20
  validation_split: 0.15

paths:
  data_dir: "/g/data/ey6/mai/PhD/GitHub_cVAE/FS_WMH_mixedSex_BP/"
  output_dir: "/g/data/ey6/mai/PhD/GitHub_cVAE/AAE_output_direct/"

device:
  gpu: true
  